{
    "sEcho":<%= params[:sEcho].to_json.html_safe || -1.to_json.html_safe %>,
    "iTotalRecords":<%= Solver.count.to_json.html_safe %>,
    "iTotalDisplayRecords":<%= @total_solvers.to_json.html_safe %>,
    "aaData":<%= @solvers.collect do |solver|
                   [ check_box_tag("cb#{solver.id}"),
                     link_to(solver.pid, solver),
                     link_to(solver.created_at, solver),
                     link_to(solver.current_state.name, solver)]
                 end.to_json.html_safe %>
}