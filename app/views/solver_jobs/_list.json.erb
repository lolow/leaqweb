{
    "sEcho":<%= params[:sEcho].to_json.html_safe || -1.to_json.html_safe %>,
    "iTotalRecords":<%= SolverJob.count.to_json.html_safe %>,
    "iTotalDisplayRecords":<%= @total_solver_jobs.to_json.html_safe %>,
    "aaData":<%= @solver_jobs.collect do |solver|
                   [ check_box_tag("cb#{solver.id}"),
                     link_to(solver.language, solver),
                     link_to(solver.scenarios, solver),
                     link_to(solver.created_at, solver),
                     link_to(solver.etem_solver.read(:state), solver)]
                 end.to_json.html_safe %>
}