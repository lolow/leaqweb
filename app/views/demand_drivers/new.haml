.grid_3
  .box(class="action")
    %h2 Actions
    %br
    %p= link_to 'Browse all', demand_drivers_path, :class => "action browse"

.grid_9
  .box
    %h2 New demand driver
    .block
      = simple_form_for @demand_driver do |f|
        = f.input :name
        = f.button :submit, :class => "aligned"
.clear