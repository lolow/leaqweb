{
    "sEcho":<%= params[:sEcho].to_json.html_safe || -1.to_json.html_safe %>,
    "iTotalRecords":<%= Technology.count.to_json.html_safe %>,
    "iTotalDisplayRecords":<%= @total_technologies.to_json.html_safe %>,
    "aaData":<%= @technologies.collect do |c|
                   [ check_box_tag("cb#{c.id}"),
                     link_to(c.name, edit_technology_path(c)),
                     truncate(c.description, :omission => "...", :length => 100)]
                 end.to_json.html_safe %>
}