{
    "sEcho":<%= params[:sEcho].to_json.html_safe || -1.to_json.html_safe %>,
    "iTotalRecords":<%= Scenario.count.to_json.html_safe %>,
    "iTotalDisplayRecords":<%= @total_scenarios.to_json.html_safe %>,
    "aaData":<%= @scenarios.collect do |o|
                   [ check_box_tag("cb#{o.id}","1",false,:disabled => (o.name=="BASE")),
                     link_to(o.name, o) ]
                 end.to_json.html_safe %>
}